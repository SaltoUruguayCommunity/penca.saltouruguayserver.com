---
import { getSession } from "auth-astro/server";
import Layout from "../../layouts/Layout.astro";
import PencasAdmin from "../../components/pencas/admin/PencasAdmin.tsx";

const { user } = Astro.locals;
if (!user?.is_admin) {
  return Astro.redirect("/", 302);
}
---

<Layout
  title="Admin"
  description="Panel de administración de la Penca Mundial 2026."
  noIndex
>
  <div class="min-h-screen">
    <nav
      class="fixed top-0 left-0 right-0 z-50 bg-surface/80 backdrop-blur-xl border-b border-accent-border/30"
    >
      <div
        class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center h-16"
      >
        <a
          href="/pronosticos"
          class="flex items-center gap-2 font-barlow font-black text-lg tracking-tight group"
        >
          <span
            class="text-accent transition-transform duration-300 group-hover:scale-110"
            >⚽</span
          >
          <span class="text-white">LA</span>
          <span class="text-accent">PENCA</span>
        </a>
        <div class="ml-auto flex items-center gap-3">
          <a
            href="/pronosticos"
            class="text-xs font-semibold uppercase tracking-wider text-muted hover:text-white transition font-teko group"
          >
            <span
              class="inline-block transition-transform duration-200 group-hover:-translate-x-1"
              >←</span
            > Volver
          </a>
        </div>
      </div>
    </nav>
    <div class="pt-20">
      <PencasAdmin client:load user={user} />
    </div>
    <footer
      class="relative border-t border-white/10 bg-[#050505] py-10 mt-8 overflow-hidden"
    >
      <div
        class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[600px] h-[200px] rounded-full pointer-events-none"
        style="background: rgba(139,92,246,0.05); filter: blur(80px);"
      >
      </div>
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div
          class="flex flex-col md:flex-row items-center justify-between gap-6"
        >
          <a
            href="/"
            class="flex items-center gap-2 font-barlow font-black text-lg group"
          >
            <span
              class="text-accent transition-transform duration-300 group-hover:scale-110"
              >⚽</span
            >
            <span class="text-white">LA</span>
            <span class="text-accent">PENCA</span>
          </a>
          <p class="text-white/30 text-xs font-mono">
            Admin Panel &mdash; Desarrollado por <a
              href="https://instagram.com/alexitoo_uy"
              target="_blank"
              rel="noopener noreferrer"
              class="text-white/60 hover:text-accent transition">Alexitoo_UY</a
            >
          </p>
        </div>
      </div>
    </footer>
  </div>
</Layout>
